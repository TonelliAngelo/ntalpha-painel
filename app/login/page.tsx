'use client';
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {supabase} from '@/lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [senha,setSenha]=useState('');const [erro,setErro]=useState('');const r=useRouter();async function entrar(e:FormEvent){e.preventDefault();setErro('');const {error}=await supabase().auth.signInWithPassword({email,password:senha});if(error)return setErro('Usuário ou senha inválidos.');r.push('/')}return <main className="login"><form className="login-card" onSubmit={entrar}><div className="brand center">NT <b>ALPHA</b><small>PAINEL IMOBILIÁRIO</small></div><h1>Acesso ao painel</h1><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" required value={senha} onChange={e=>setSenha(e.target.value)}/></label>{erro&&<p className="error">{erro}</p>}<button>Entrar</button></form></main>}
